# Copy this file to .env (or export these as real environment variables) before
# running the backend. Never commit the real .env file.

# Required. OpenAI API key used for answer generation (gpt-4o-mini by default).
# Ingestion and retrieval work without this key; only /api/query needs it.
OPENAI_API_KEY=
